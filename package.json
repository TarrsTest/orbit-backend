{
  "name": "evalkit",
  "version": "0.3.0",
  "private": true,
  "description": "Small utility kit used as the Coloom pod-eval fixture. No dependencies; tests use node --test.",
  "scripts": {
    "test": "node --test test/"
  }
}
