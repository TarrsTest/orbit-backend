'use strict';

/**
 * Parse a duration like "1h30m", "45s", "2h", "1h5m10s" or "250ms" into
 * milliseconds. TODO: not implemented yet.
 */
function parseDuration(text) {
  throw new Error('not implemented');
}

module.exports = { parseDuration };
