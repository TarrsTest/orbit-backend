'use strict';

/**
 * Compare two semantic version strings ("1.2.3", "1.2.3-beta.1").
 * Returns -1, 0 or 1. TODO: not implemented yet.
 */
function compareVersions(a, b) {
  throw new Error('not implemented');
}

module.exports = { compareVersions };
